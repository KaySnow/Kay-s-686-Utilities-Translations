<Guide id="install_lspdfr" name="How to Install LSPDFR">
  <Author>Charlie686</Author>

  <Page id="home">
    <Title>How do I install LSPDFR?</Title>
    <Description>
To begin, use the 'Download LSPDFR' button which will take you to the LSPD First Response page.
When downloading, ensure to choose the **Manual Install**.
</Description>
    <ImageUrl>https://i.imgur.com/M6o9uAJ.png</ImageUrl>
    <Fields>
      <Field name="What about RAGEPluginHook?" inline="true">
RAGEPluginHook is already included in the LSPDFR download itself.
</Field>
    </Fields>
    <Buttons>
      <Button type="interactive" targetPage="manualinstall">Installing the Manual Install</Button>
      <Button type="link" url="https://www.lcpdfr.com/downloads/gta5mods/g17media/7792-lspd-first-response/">Download LSPDFR</Button>
    </Buttons>
  </Page>

  <Page id="manualinstall">
    <Title>Installing LSPDFR via the Manual Install.</Title>
    <Description>
Open the LSPD First Response manual install, and you should see several files and a few folders.

Drag and drop all files and folders except the `RPH_Readme.txt` file and the `Licenses` folder into your GTAV folder.

**If prompted, click Replace All Files.**
</Description>
    <ImageUrl>https://i.imgur.com/UpFP9YY.png</ImageUrl>
    <Buttons>
      <Button type="interactive" targetPage="firsttime">Run LSPDFR for the first time</Button>
    </Buttons>
  </Page>

  <Page id="firsttime">
    <Title>Run LSPDFR for the first time.</Title>
    <Description>
Launch `RAGEPluginHook.exe` in your GTAV folder.
It will take you through the first-time setup, and then launch GTAV.
</Description>
    <Fields>
      <Field name="Before you play, make sure:" inline="false">
You have already completed the prologue for GTAV. LSPDFR requires you to be in free roam to work correctly.
</Field>
      <Field name="I am in game with RAGEPluginHook, however LSPDFR is not loaded?" inline="false">
You may need to load it in game. Open the RPH console (F4 by default), and type in `LoadPlugin LSPD First Response.dll`.
</Field>
    </Fields>
  </Page>
</Guide>
